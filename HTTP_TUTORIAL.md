# HTTP、URL 解析和路由 - 从零开始教程

## 📚 目录
1. [什么是 HTTP？](#什么是-http)
2. [HTTP 请求和响应](#http-请求和响应)
3. [URL 详解](#url-详解)
4. [URL 解析](#url-解析)
5. [路由是什么？](#路由是什么)
6. [项目中的实现](#项目中的实现)
7. [实际例子](#实际例子)

---

## 什么是 HTTP？

**HTTP (HyperText Transfer Protocol)** 是互联网上应用最广泛的网络协议，用于在浏览器和服务器之间传输数据。

### 简单理解
想象你在餐厅点餐：
- **浏览器** = 你（顾客）
- **服务器** = 餐厅
- **HTTP** = 点餐的规则和流程

### HTTP 的特点
- **请求-响应模式**：浏览器发送请求，服务器返回响应
- **无状态**：每次请求都是独立的（服务器不记得你上次点了什么）
- **基于文本**：人类可读的格式

---

## HTTP 请求和响应

### HTTP 请求（Request）

当你在浏览器输入网址并回车时，浏览器会发送一个 HTTP 请求：

```
GET /index.html HTTP/1.1
Host: www.example.com
User-Agent: Mozilla/5.0...
Accept: text/html
```

**请求的组成部分：**

1. **请求行**（第一行）
   - `GET` - HTTP 方法（GET、POST、PUT、DELETE 等）
   - `/index.html` - 请求的路径（URL 的一部分）
   - `HTTP/1.1` - HTTP 版本

2. **请求头**（Headers）
   - `Host`: 服务器地址
   - `User-Agent`: 浏览器信息
   - `Accept`: 客户端能接受的内容类型
   - `Content-Type`: 请求体的类型（POST 请求常用）
   - `Content-Length`: 请求体的长度

3. **请求体**（Body，可选）
   - GET 请求通常没有请求体
   - POST 请求的请求体包含要提交的数据

### HTTP 响应（Response）

服务器收到请求后，会返回一个 HTTP 响应：

```
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8
Content-Length: 1234

<!DOCTYPE html>
<html>...</html>
```

**响应的组成部分：**

1. **状态行**（第一行）
   - `HTTP/1.1` - HTTP 版本
   - `200` - 状态码（200=成功，404=未找到，500=服务器错误）
   - `OK` - 状态文本

2. **响应头**（Headers）
   - `Content-Type`: 响应内容的类型
   - `Content-Length`: 响应体的长度
   - `Set-Cookie`: 设置 Cookie（用于保持登录状态）

3. **响应体**（Body）
   - 实际的内容（HTML、JSON、图片等）

### 常见的 HTTP 方法

| 方法 | 用途 | 例子 |
|------|------|------|
| **GET** | 获取资源 | 查看网页、获取数据 |
| **POST** | 提交数据 | 登录、注册、提交表单 |
| **PUT** | 更新资源 | 更新用户信息 |
| **DELETE** | 删除资源 | 删除文章 |

### 常见的状态码

| 状态码 | 含义 | 例子 |
|--------|------|------|
| **200** | 成功 | 页面正常显示 |
| **404** | 未找到 | 访问不存在的页面 |
| **500** | 服务器错误 | 服务器代码出错 |
| **400** | 请求错误 | 请求格式不对 |

---

## URL 详解

**URL (Uniform Resource Locator)** 就是网址，比如：
```
http://www.example.com:8080/path/to/page?name=张三&age=20#section1
```

### URL 的组成部分

```
http://www.example.com:8080/path/to/page?name=张三&age=20#section1
│    │                │    │              │                    │
│    │                │    │              │                    └─ 锚点（页面内跳转）
│    │                │    │              └─ 查询参数（Query String）
│    │                │    └─ 路径（Path）
│    │                └─ 端口号（默认80，HTTPS默认443）
│    └─ 主机名（域名或IP）
└─ 协议（http 或 https）
```

**详细解释：**

1. **协议** (`http://` 或 `https://`)
   - `http`: 普通协议
   - `https`: 加密协议（更安全）

2. **主机名** (`www.example.com`)
   - 服务器的地址
   - 可以是域名或 IP 地址（如 `127.0.0.1`）

3. **端口号** (`:8080`)
   - 服务器上服务的端口
   - HTTP 默认 80，HTTPS 默认 443
   - 你的项目使用 8080

4. **路径** (`/path/to/page`)
   - 服务器上资源的路径
   - 类似文件系统中的路径

5. **查询参数** (`?name=张三&age=20`)
   - 以 `?` 开头
   - 多个参数用 `&` 连接
   - 格式：`key=value`

6. **锚点** (`#section1`)
   - 用于页面内跳转
   - 服务器通常不处理，由浏览器处理

### URL 示例解析

```
http://localhost:8080/register?username=test&password=123
```

- **协议**: `http`
- **主机名**: `localhost`（本地服务器）
- **端口**: `8080`
- **路径**: `/register`
- **查询参数**: `username=test&password=123`

---

## URL 解析

URL 解析就是把一个完整的 URL 字符串拆解成各个部分。

### 为什么要解析 URL？

服务器需要知道：
- 用户想访问哪个页面？（路径）
- 用户传递了什么参数？（查询参数）
- 用户想做什么操作？（HTTP 方法）

### 解析过程

**原始 URL:**
```
http://localhost:8080/login?username=admin&password=123456
```

**解析后得到：**
```cpp
{
    method: "GET",           // HTTP 方法
    path: "/login",          // 路径
    route: "/login",         // 路由（去掉查询参数后的路径）
    query_string: "username=admin&password=123456",  // 查询字符串
    params: {                // 解析后的参数
        "username": "admin",
        "password": "123456"
    }
}
```

### URL 编码/解码

URL 中不能直接包含某些字符（如中文、空格），需要编码：

- **空格** → `%20` 或 `+`
- **中文"张"** → `%E5%BC%A0`
- **&** → `%26`

**例子：**
```
原始: http://example.com/search?q=你好 世界
编码后: http://example.com/search?q=%E4%BD%A0%E5%A5%BD+%E4%B8%96%E7%95%8C
```

---

## 路由是什么？

**路由（Routing）** 就是根据 URL 路径，决定执行哪个处理函数。

### 简单理解

想象一个电话总机：
- 你打电话说"我要找销售部" → 总机转接到销售部
- 你打电话说"我要找客服" → 总机转接到客服

路由就是 HTTP 服务器的"总机"，根据 URL 路径决定调用哪个处理函数。

### 路由表

```
URL 路径          →  处理函数
─────────────────────────────────
/                 →  show_homepage()
/register         →  handle_register()
/login            →  handle_login()
/user/profile     →  show_user_profile()
```

### 路由匹配

当用户访问 `/login` 时：
1. 服务器解析 URL，得到路径 `/login`
2. 查找路由表，找到对应的处理函数
3. 执行处理函数，返回响应

---

## 项目中的实现

### 1. HTTP 请求结构（server.h）

```cpp
struct HttpRequest {
    std::string method;              // HTTP 方法（GET、POST等）
    std::string path;                // 完整路径（包含查询参数）
    std::string route;               // 路由（去掉查询参数）
    std::string query_string;        // 查询字符串（?后面的部分）
    std::map<std::string, std::string> headers;  // 请求头
    std::string body;                // 请求体（POST数据）
    std::map<std::string, std::string> params;  // 解析后的参数
};
```

### 2. HTTP 响应结构

```cpp
struct HttpResponse {
    int status_code = 200;           // 状态码（200=成功）
    std::string status_text = "OK";  // 状态文本
    std::string content_type = "application/json; charset=utf-8";  // 内容类型
    std::string body;                // 响应体（实际内容）
};
```

### 3. URL 解析函数

**解析 HTTP 请求行：**
```cpp
// 从 "GET /login?username=test HTTP/1.1" 中提取：
// - method: "GET"
// - path: "/login?username=test"
```

**解析查询参数：**
```cpp
// 从 "username=test&password=123" 中提取：
// params["username"] = "test"
// params["password"] = "123"
```

**URL 解码：**
```cpp
// 将 "%E4%BD%A0%E5%A5%BD" 解码为 "你好"
```

### 4. 路由处理函数

```cpp
HttpResponse handle_route(const HttpRequest &request) {
    HttpResponse response;
    
    // 根据 HTTP 方法分类处理
    if (request.method == "POST") {
        // POST 请求处理
        if (request.route == "/register") {
            // 处理注册
            response.body = register_user(...);
        }
        else if (request.route == "/login") {
            // 处理登录
            response.body = login_user(...);
        }
    }
    else if (request.method == "GET") {
        // GET 请求处理
        if (request.route == "/") {
            // 返回首页
            response.body = get_index_html();
        }
        else if (request.route == "/favicon.ico") {
            // 返回图标（或204无内容）
            response.status_code = 204;
        }
    }
    
    return response;
}
```

---

## 实际例子

### 例子 1：访问首页

**用户在浏览器输入：**
```
http://localhost:8080/
```

**浏览器发送的 HTTP 请求：**
```
GET / HTTP/1.1
Host: localhost:8080
User-Agent: Mozilla/5.0...
```

**服务器处理过程：**
1. 解析请求：`method="GET"`, `path="/"`, `route="/"`
2. 路由匹配：找到 `route == "/"` 的处理
3. 执行：调用 `get_index_html()` 获取 HTML
4. 返回响应：
```
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8
Content-Length: 4032

<!DOCTYPE html>
<html>...</html>
```

### 例子 2：用户注册

**用户在表单填写并提交：**
- 用户名：`testuser`
- 密码：`testpass123`

**浏览器发送的 HTTP 请求：**
```
POST /register HTTP/1.1
Host: localhost:8080
Content-Type: application/x-www-form-urlencoded
Content-Length: 38

username=testuser&password=testpass123
```

**服务器处理过程：**
1. 解析请求：
   - `method="POST"`
   - `path="/register"`
   - `route="/register"`
   - `body="username=testuser&password=testpass123"`
   - `params["username"]="testuser"`
   - `params["password"]="testpass123"`

2. 路由匹配：找到 `route == "/register"` 的处理

3. 执行业务逻辑：
   ```cpp
   std::string username = request.params["username"];  // "testuser"
   std::string password = request.params["password"];  // "testpass123"
   response.body = register_user(username, password);
   ```

4. 返回响应：
```
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
Content-Length: 44

{"success": true, "message": "注册成功"}
```

### 例子 3：带查询参数的 GET 请求

**用户在浏览器输入：**
```
http://localhost:8080/search?keyword=hello&page=1
```

**浏览器发送的 HTTP 请求：**
```
GET /search?keyword=hello&page=1 HTTP/1.1
Host: localhost:8080
```

**服务器解析结果：**
```cpp
{
    method: "GET",
    path: "/search?keyword=hello&page=1",
    route: "/search",
    query_string: "keyword=hello&page=1",
    params: {
        "keyword": "hello",
        "page": "1"
    }
}
```

---

## 完整流程图

```
用户输入 URL
    ↓
浏览器发送 HTTP 请求
    ↓
服务器接收请求
    ↓
解析 HTTP 请求（提取 method、path、headers、body）
    ↓
解析 URL（提取 route、query_string、params）
    ↓
路由匹配（根据 route 找到处理函数）
    ↓
执行业务逻辑（处理数据、查询数据库等）
    ↓
构建 HTTP 响应（status_code、headers、body）
    ↓
发送响应给浏览器
    ↓
浏览器显示结果
```

---

## 关键概念总结

1. **HTTP** = 浏览器和服务器通信的协议
2. **URL** = 网址，包含协议、主机、路径、参数等
3. **请求** = 浏览器发送给服务器的数据
4. **响应** = 服务器返回给浏览器的数据
5. **路由** = 根据 URL 路径选择处理函数
6. **解析** = 把字符串拆解成有用的信息

---

## 练习建议

1. **观察浏览器开发者工具**
   - 按 F12 打开开发者工具
   - 查看 Network（网络）标签
   - 观察请求和响应的详细信息

2. **测试不同的 URL**
   - `http://localhost:8080/`
   - `http://localhost:8080/register`
   - `http://localhost:8080/login?username=test`

3. **理解项目代码**
   - 查看 `parse_http_request()` 函数
   - 查看 `handle_route()` 函数
   - 理解数据是如何流转的

---

希望这个教程能帮你理解 HTTP、URL 解析和路由的基本概念！如果有任何问题，随时问我。

