# WebServer - 高并发网络服务器

一个面向高并发连接场景的网络服务项目，基于 Reactor 事件驱动架构（epoll LT，1-N），通过线程池、内存池、连接池等技术降低系统开销并提升稳定性。

## 📋 项目简介

本项目是一个高性能的 HTTP 服务器实现，采用现代 C++ 开发，支持高并发连接处理。服务器使用 Reactor 事件驱动模式，结合非阻塞 I/O、线程池、内存池和 MySQL 连接池等技术，实现了单机 2 核压测 QPS ≈ 35k 的性能表现。

### 核心特性

- ✅ **Reactor 事件驱动架构**：基于 epoll LT 模式，1-N 模型（单线程处理 I/O，线程池处理业务）
- ✅ **非阻塞 I/O**：所有 socket 设置为非阻塞模式，提高并发性能
- ✅ **线程池**：固定线程数 + 任务队列 + 条件变量调度，支持优雅退出与线程回收
- ✅ **内存池**：定长块复用、缓存行对齐、空闲链表管理、动态扩容，支持批量分配/回收
- ✅ **MySQL 连接池**：最小/最大连接数、超时等待、空闲清理、动态扩容、RAII 归还
- ✅ **日志系统**：线程安全的日志系统，支持多级别日志输出
- ✅ **HTTP 协议支持**：完整的 HTTP 请求解析和响应构建
- ✅ **用户管理系统**：提供用户注册、登录、密码修改等 RESTful API

## 🛠️ 技术栈

- **语言**: C++14
- **I/O 多路复用**: epoll (Linux)
- **数据库**: MySQL
- **加密库**: OpenSSL (SHA256)
- **并发模型**: 多线程 + 事件驱动
- **设计模式**: RAII、单例、工厂模式
- **标准库**: STL (容器、算法、智能指针)

## 📁 项目结构

```
WebServ'er/
├── main.cpp              # 程序入口
├── server.h              # 服务器主类（HTTP处理、路由、业务逻辑）
├── Reactor.h             # Reactor事件驱动架构实现
├── Thread_Pool.h         # 线程池实现
├── Memory_Pool.h         # 内存池实现
├── Mysql_Pool.h          # MySQL连接池实现
├── Logger.h               # 日志系统
├── Makefile              # 编译配置
├── wwwroot/              # 静态资源目录
│   └── index.html        # 首页HTML
├── README.md             # 项目说明文档
├── IMPROVEMENTS.md       # 改进说明文档
└── LICENSE               # 许可证
```

## 🏗️ 架构设计

### Reactor 模式架构

```
┌─────────────────────────────────────────┐
│         主线程（事件循环）                │
│  ┌───────────────────────────────────┐  │
│  │      Reactor (epoll事件循环)       │  │
│  │  - 监听服务器socket                │  │
│  │  - 处理客户端连接事件              │  │
│  │  - 分发I/O事件                     │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
              │
              ├─── 新连接 ───┐
              │              │
              ├─── 读事件 ───┤
              │              │
              └─── 写事件 ───┘
                            │
                            ▼
              ┌─────────────────────────┐
              │    线程池（业务处理）     │
              │  - HTTP请求解析          │
              │  - 路由处理              │
              │  - 数据库操作            │
              │  - 响应构建              │
              └─────────────────────────┘
```

### 数据流

1. **连接建立**: 客户端连接 → Reactor 注册 → 非阻塞 socket
2. **请求处理**: epoll 读事件 → 读取数据 → 线程池处理 → 构建响应
3. **响应发送**: 写入缓冲区 → epoll 写事件 → 非阻塞发送 → 关闭连接

## 🚀 快速开始

### 环境要求

- **操作系统**: Linux (推荐 Ubuntu 20.04+ 或 CentOS 7+)
- **编译器**: GCC 5.0+ (支持 C++14)
- **依赖库**:
  - MySQL Client Library (`libmysqlclient-dev`)
  - OpenSSL (`libssl-dev`)
  - pthread (系统自带)

### 安装依赖

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install -y g++ libmysqlclient-dev libssl-dev
```

**CentOS/RHEL:**
```bash
sudo yum install -y gcc-c++ mysql-devel openssl-devel
```

### 编译项目

```bash
# 使用 Makefile 编译
make

# 或手动编译
g++ -std=c++14 -O2 -Wall -pthread main.cpp -o webserver -lmysqlclient -lssl -lcrypto

# 编译调试版本
make debug
```

### 配置数据库

1. 创建数据库：
```sql
CREATE DATABASE webserver CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 修改 `main.cpp` 中的数据库配置：
```cpp
config.mysql_host = "localhost";
config.mysql_port = 3306;
config.mysql_user = "root";
config.mysql_password = "your_password";
config.mysql_db_name = "webserver";
```

### 运行服务器

```bash
# 直接运行
./webserver

# 或使用 Makefile
make run

# 后台运行
nohup ./webserver > webserver.out 2>&1 &
echo $! > webserver.pid
```

### 停止服务器

```bash
# 前台运行：按 Ctrl+C

# 后台运行：使用进程ID
kill $(cat webserver.pid)

# 或使用端口查找进程
sudo fuser -k 8080/tcp
```

## ⚙️ 配置说明

### ServerConfig 配置项

```cpp
struct ServerConfig {
    // 内存池配置
    size_t max_memory = 1024 * 1024 * 1024;  // 最大内存（1GB）
    bool enable_memory_pool_tls = true;      // 启用内存池TLS

    // MySQL连接池配置
    std::string mysql_host = "localhost";
    int mysql_port = 3306;
    std::string mysql_user;
    std::string mysql_password;
    std::string mysql_db_name;
    size_t mysql_min_connections = 5;
    size_t mysql_max_connections = 20;

    // 线程池配置
    size_t thread_pool_min_threads = 5;
    size_t thread_pool_max_threads = 20;

    // HTTP服务器配置
    int http_port = 8080;
    std::string http_host = "0.0.0.0";
    int listen_backlog = 128;
};
```

## 📡 API 文档

### 用户注册

**请求:**
```http
POST /register
Content-Type: application/x-www-form-urlencoded

username=testuser&password=testpass123
```

**响应:**
```json
{
  "success": true,
  "message": "注册成功"
}
```

### 用户登录

**请求:**
```http
POST /login
Content-Type: application/x-www-form-urlencoded

username=testuser&password=testpass123
```

**响应:**
```json
{
  "success": true,
  "message": "登录成功"
}
```

### 修改密码

**请求:**
```http
POST /change_password
Content-Type: application/x-www-form-urlencoded

username=testuser&old_password=oldpass&new_password=newpass
```

**响应:**
```json
{
  "success": true,
  "message": "密码修改成功"
}
```

### 首页

**请求:**
```http
GET /
```

**响应:** HTML 页面（用户管理系统界面）

## 📊 性能指标

- **单机 2 核压测**: QPS ≈ 35,000
- **并发连接**: 支持高并发连接（受系统限制）
- **内存效率**: 使用内存池减少分配开销
- **CPU 效率**: 事件驱动减少线程切换

### 性能优化特性

1. **事件驱动**: 单线程处理所有 I/O，减少线程切换开销
2. **非阻塞 I/O**: 提高并发性能，避免线程阻塞
3. **内存池**: 减少内存分配开销，TLS 缓存减少锁竞争
4. **连接池**: 数据库连接复用，减少连接建立开销
5. **动态线程管理**: 根据负载动态调整线程数

## 🔧 核心组件详解

### 1. Reactor 事件驱动架构

- **文件**: `Reactor.h`
- **模式**: epoll LT (Level Triggered)
- **特点**:
  - 单线程事件循环处理所有 I/O 事件
  - 非阻塞 socket 操作
  - 自动连接生命周期管理
  - 支持读/写事件分离

### 2. 线程池

- **文件**: `Thread_Pool.h`
- **特性**:
  - 固定线程数 + 任务队列
  - 条件变量调度
  - 支持优雅退出与线程回收
  - 动态线程管理（最小/最大线程数）

### 3. 内存池

- **文件**: `Memory_Pool.h`
- **特性**:
  - 定长块复用（8, 16, 32, 64, 128, 256, 512, 1024 字节）
  - 缓存行对齐
  - 空闲链表管理
  - 动态扩容
  - TLS 缓存减少锁竞争

### 4. MySQL 连接池

- **文件**: `Mysql_Pool.h`
- **特性**:
  - 最小/最大连接数控制
  - 超时等待机制
  - 空闲连接清理
  - 动态扩容
  - RAII 自动归还连接
  - 失效连接重建

### 5. 日志系统

- **文件**: `Logger.h`
- **特性**:
  - 线程安全
  - 多级别日志（DEBUG, INFO, WARN, ERROR）
  - 带时间戳输出
  - 可配置最小日志级别

## 🧪 测试

### 功能测试

```bash
# 测试首页
curl http://localhost:8080/

# 测试用户注册
curl -X POST http://localhost:8080/register \
  -d "username=testuser&password=testpass123" \
  -H "Content-Type: application/x-www-form-urlencoded"

# 测试用户登录
curl -X POST http://localhost:8080/login \
  -d "username=testuser&password=testpass123" \
  -H "Content-Type: application/x-www-form-urlencoded"
```

### 性能测试

使用 `ab` (Apache Bench) 或 `wrk` 进行压测：

```bash
# 安装 ab
sudo apt-get install apache2-utils

# 压测
ab -n 10000 -c 100 http://localhost:8080/
```

## 📝 开发指南

### 添加新路由

在 `server.h` 的 `handle_route()` 函数中添加：

```cpp
else if (request.route == "/new_route") {
    // 处理逻辑
    response.body = "处理结果";
}
```

### 添加新的业务逻辑

1. 在 `Server` 类中添加私有方法
2. 在 `handle_route()` 中调用
3. 使用线程池处理耗时操作

### 日志使用

```cpp
Logger::debug("调试信息");
Logger::info("一般信息");
Logger::warn("警告信息");
Logger::error("错误信息");
```

## ❓ 常见问题

### Q: 编译时提示找不到 mysql.h？

**A:** 需要安装 MySQL 开发库：
```bash
sudo apt-get install libmysqlclient-dev  # Ubuntu/Debian
sudo yum install mysql-devel            # CentOS/RHEL
```

### Q: 运行时提示端口被占用？

**A:** 检查并关闭占用端口的进程：
```bash
sudo lsof -iTCP:8080 -sTCP:LISTEN
sudo fuser -k 8080/tcp
```

### Q: 数据库连接失败？

**A:** 检查：
1. MySQL 服务是否运行
2. 数据库配置是否正确
3. 数据库用户权限是否足够
4. 防火墙是否允许连接

### Q: 如何查看服务器统计信息？

**A:** 在代码中调用：
```cpp
std::string stats = server.get_stats();
std::cout << stats << std::endl;
```

## 🔮 后续优化方向

- [ ] HTTP/1.1 Keep-Alive 支持
- [ ] HTTP/2 支持
- [ ] SSL/TLS (HTTPS) 支持
- [ ] 负载均衡（多进程/多服务器）
- [ ] Redis 缓存支持
- [ ] 更详细的性能监控
- [ ] 配置文件支持（JSON/YAML）
- [ ] 单元测试和集成测试

## 📄 许可证

查看 [LICENSE](LICENSE) 文件了解详情。

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题或建议，请通过 Issue 反馈。

---

**注意**: 本项目仅用于学习和研究目的。在生产环境使用前，请进行充分的安全测试和性能优化。

